"""Task 002A sequence-clustered-partition pipeline (implementation and tiny
fixture tests only).

Task 002B (real dataset decode, real MMseqs2 clustering) and Task 002C (real
partition assignment/audit) are separate, later execution steps, each gated
on its own planning-review acceptance. Nothing in this package touches the
real CSV, the real dataset FASTAs, or a real component/partition.
"""
